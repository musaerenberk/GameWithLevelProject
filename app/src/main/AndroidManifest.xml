<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mebk.exercise1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Exercise1">
        <activity android:name=".GameScreen7"></activity>
        <activity android:name=".GameScreen6" />
        <activity android:name=".GameScreen5" />
        <activity android:name=".GameScreen4" />
        <activity android:name=".GameScreen3" />
        <activity android:name=".GameScreen2" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".GameScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>